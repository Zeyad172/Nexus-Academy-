export interface Certificate {
  certificate_id: string;
  user_id: number;
  course_id: number;
  issue_date: string;
  course_name: string;
  inst_first: string;
  inst_last: string;
  download_url: string;
}
